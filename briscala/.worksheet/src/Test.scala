
import org.mmarini.briscala._
import scala.util.Random
import scala.collection.mutable.Set
import com.sun.org.apache.xml.internal.serializer.ToStream

object Test {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(184); val res$0 = 
  (false).toString;System.out.println("""res0: String = """ + $show(res$0));$skip(24); val res$1 = 
  List(1, 2, 3, 4).init;System.out.println("""res1: List[Int] = """ + $show(res$1))}
}
