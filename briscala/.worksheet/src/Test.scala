
import org.mmarini.briscala._
import scala.util.Random
import scala.collection.mutable.Set

object Test {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(115); val res$0 = 
  0 to 2;System.out.println("""res0: scala.collection.immutable.Range.Inclusive = """ + $show(res$0))}
}
